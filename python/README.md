# How to use this app

* Execute the command `pip install --trusted-host pypi.python.org -r requirements.txt`.
* Excute the Python script by using the command `python app.py`.
* Open your browser on `http://localhost:80/`.