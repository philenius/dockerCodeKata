# How to use this app

* Copy the file `main.go` to the directory _~/go/src/hello-world/_.
* Set the environment variable `GOPATH` to `$HOME/go`.
* Download all dependencies by using the command `go get`.
* Run the command `go build`.
* Execute the binary `./hello-world`
* Visit `http://localhost:8080/` in your browser.